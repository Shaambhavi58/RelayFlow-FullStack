name: RelayFlow CI

on:
  push:
  pull_request:

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install backend dependencies
        run: pip install -e '.[dev]'

      - name: Run Ruff
        run: ruff check relayflow tests

      - name: Run backend tests
        run: pytest -q

  frontend:
    name: Frontend build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Install Rolldown Linux native binding
        shell: bash
        run: |
          ROLLDOWN_VERSION=$(node -p "require('./node_modules/rolldown/package.json').version")
          echo "Installing Rolldown Linux binding version ${ROLLDOWN_VERSION}"
          npm install --no-save --no-package-lock "@rolldown/binding-linux-x64-gnu@${ROLLDOWN_VERSION}"

      - name: Ensure frontend scripts are executable
        shell: bash
        run: chmod +x scripts/*.sh

      - name: Build frontend
        run: npm run build

  docker:
    name: Docker validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Docker Compose
        run: docker compose -f backend/docker-compose.yml config

      - name: Build backend Docker image
        run: docker build backend -t relayflow-api:ci